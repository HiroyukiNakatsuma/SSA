<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.ssa.ssa.mapper.AccountJoinRoomMapper">

    <insert id="insert">
        INSERT INTO
        AccountJoinRoom (
            accountId,
            roomId,
            roomRole
        ) VALUES (
            #{accountId},
            #{roomId},
            ${@com.example.ssa.ssa.enums.RoomRole@OWNER.code}
        )
    </insert>

</mapper>